# Development environment only - use secure secrets in production
# For Codespaces, this password is only accessible within the container
# In production/Codespaces, use: ${MSSQL_SA_PASSWORD} from repository secrets
MSSQL_SA_PASSWORD=${MSSQL_SA_PASSWORD:-P@ssw0rd!}